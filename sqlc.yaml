version: "2"
sql:
  - engine: "sqlite"
    queries: ["queries.sql"]
    schema: "schema.sql"
    codegen: [{ out: ".", plugin: "py" }]
plugins:
  - name: "py"
    process: { cmd: "sqlc-gen-python" }
    env: ["LIXUE_ROOT", "PATH", "LD_LIBRARY_PATH", "PYTHONPATH"]
